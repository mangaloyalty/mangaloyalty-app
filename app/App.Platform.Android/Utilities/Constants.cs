namespace App.Platform.Android.Utilities
{
    public static class Constants
    {
        // Initialize the channel constants.
        public static string ChannelServiceKey = "ServiceChannel";
        public static string ChannelServiceName = "Service";

        // Initialize the notification constants.
        public static int NotificationId = 1337;
        public static string NotificationActive = "The service is active.";
        public static string NotificationKilled = "The service has been killed.";
    }
}